import PageThreadLayout from '../layouts/PageThread';
import { Page } from 'core';

class PageThread extends Page {
  constructor() {
    super('pageThread', PageThreadLayout);
  }
}

export default PageThread;
