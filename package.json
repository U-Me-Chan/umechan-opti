{
  "name": "my-workspaces-powered-project",
  "workspaces": [
    "core",
    "bff",
    "frontend"
  ],
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "prepare": "husky install"
  }
}
